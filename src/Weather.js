import React from "react";

import { Rings } from "react-loader-spinner";

export default function Weather() {
  return (
    <div className="search">
      Ready to build my app
      <Rings color="#00BFFF" height={80} width={80} />
    </div>
  );
}
